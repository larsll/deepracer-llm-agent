/**
 * This is a compatibility layer that re-exports the new modular Bedrock implementation.
 * Existing code can continue using this file as the entry point.
 */
import BedrockService from "./bedrock/index";

export default BedrockService;
